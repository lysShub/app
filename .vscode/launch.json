{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Wails: Dev app",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/build/bin/app.exe",
            "preLaunchTask": "build",
            "cwd": "${workspaceFolder}"
        }
    ]
}